# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'simple ios client' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'UAProgressView'
  pod 'UICountingLabel'

  # Pods for simple ios client

  target 'simple ios clientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'simple ios clientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
